# speech-emotion-detection
Speech Emotion Detection (SED) project using LSTM and CNN models to classify emotions from speech. Trained on TESS and SAVEE datasets, it detects emotions like Happy, Sad, Angry, Fear, and Neutral. The project uses data augmentation, dropout, batch normalization, and early stopping, and compares model performance using accuracy and F1-score.
